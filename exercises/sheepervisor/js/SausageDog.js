class SausageDog extends Animal {
  constructor(x, y, image, width, height) {
    super(x, y, image, width, height);
  }

  moveRandom() {
    super.moveRandom();
  }

  moveWrap() {
    super.moveWrap();
  }

  update() {
    super.update();
  }
}
